# 1. Subsets (子集)

| LeetCode | 難度 | 標籤 |
| :--- | :--- | :--- |
| [78. Subsets](https://leetcode.com/problems/subsets/) | Medium | Array, Backtracking |

## 題目描述 (Problem)

給你一個整數陣列 `nums`（所有元素互不相同），回傳該陣列的 **所有子集 (Power Set)**。
結果中不可以包含重複的子集。

### 例子 (Examples)
```cpp
Input: nums = [1, 2, 3]
Output: [[], [1], [2], [1,2], [3], [1,3], [2,3], [1,2,3]]
```

---

## 🧠 思考模式 (Thinking Process)

### 決策樹視角
對於 `nums` 中的每個元素，我們有兩個選擇：**加入當前子集** 或 **不加入**。
*   每到一個位置，當前的 `path` 都是一個合法的子集，可以加入結果。

---

## 📝 微步驟計畫 (Micro-Steps)

1.  **定義 `backtrack(start, path)`**：`start` 表示從 `nums` 的哪個索引開始考慮，`path` 是目前組成的子集。
2.  **把當前 `path` 加入結果** (不像排列問題，子集在任何時候都是合法的)。
3.  **遍歷 `i` 從 `start` 到 `nums.size()`：**
    *   **做選擇：** `path.push_back(nums[i])`。
    *   **遞迴：** `backtrack(i + 1, path)`。（從 `i+1` 開始，避免重複使用同一個元素）
    *   **撤銷選擇：** `path.pop_back()`。

---

## 💻 實作程式碼 (C++)

```cpp
#include <vector>

class Solution {
public:
    std::vector<std::vector<int>> result;

    std::vector<std::vector<int>> subsets(std::vector<int>& nums) {
        std::vector<int> path;
        backtrack(nums, 0, path);
        return result;
    }

private:
    void backtrack(std::vector<int>& nums, int start, std::vector<int>& path) {
        // 每一個路徑都是一個合法子集，無條件加入
        result.push_back(path);

        // 遍歷從 start 開始的所有選擇
        for (int i = start; i < nums.size(); ++i) {
            // 做選擇
            path.push_back(nums[i]);

            // 遞迴 (下一層從 i+1 開始)
            backtrack(nums, i + 1, path);

            // 撤銷選擇 (回溯)
            path.pop_back();
        }
    }
};
```

### 複雜度分析
*   **時間複雜度：$O(n \times 2^n)$** - 共有 $2^n$ 個子集，每個子集平均需要 $O(n)$ 時間複製到結果。
*   **空間複雜度：$O(n)$** - 遞迴深度 + `path` 長度。

---

## 🧪 自我檢測
*   [ ] 為什麼遞迴時 `start` 是 `i + 1` 而不是 `start + 1`？ (因為 `i` 是我們現在選的元素，下一層要從 `i` 的後面開始，才不會選到重複或之前的)。
